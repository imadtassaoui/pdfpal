import { FC } from "react";

interface ReaderProps {}

const Reader: FC<ReaderProps> = ({}) => {
  return <div className="w-full hidden md:flex bg-black md:h-full">Reader</div>;
};

export default Reader;
