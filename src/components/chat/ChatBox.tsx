import { FC } from "react";

interface ChatBoxProps {}

const ChatBox: FC<ChatBoxProps> = ({}) => {
  return (
    <div className="bg-primary w-full lg:w-[800px] md:h-full">ChatBox</div>
  );
};

export default ChatBox;
